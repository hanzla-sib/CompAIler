# make sure to get your own Gemini API key from Google Cloud Console and make a .env file and paste it there
# Vite only exposes environment variables that start with VITE_ to the client.
# For local development, set the VITE-prefixed variable below. Do NOT commit real keys.
VITE_GEMINI_API_KEY=your_gemini_api_key_here